package NanodeControl::Run;
use strict;
use Dancer qw(:syntax);
use base 'Exporter';

our @EXPORT = qw(enable_schedule disable_schedule);

sub enable_schedule {
  my ($url,$state,) = @_;
}

sub disable_schedule {
  my ($url,$state,) = @_;
}

